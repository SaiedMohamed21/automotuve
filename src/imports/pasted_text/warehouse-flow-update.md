I am providing you with 2 existing screens that are currently missing from the Warehouse flow.

The "New" button in the existing Warehouse screen is currently NOT WORKING.

I want you to add these 2 provided screens to the EXISTING project and connect the "New" button to the correct screen/flow.

IMPORTANT:
DO NOT create duplicate screens.
DO NOT create a separate Warehouse flow.
DO NOT rebuild the existing Warehouse screens.
Use the 2 screens I provide as the SOURCE OF TRUTH.

========================================
1. FIX THE "NEW" BUTTON
========================================

Find the existing "New" button in the Warehouse screen.

Currently:

Warehouse Screen
→ New
→ NOTHING / NOT CONNECTED

Fix this connection.

When the user clicks the existing "New" button:

→ Open the appropriate NEW screen that I am providing.

Use the correct screen based on the purpose of the button.

Do NOT create another New screen.

========================================
2. CONNECT THE 2 PROVIDED SCREENS
========================================

Add both provided screens to the EXISTING Warehouse flow.

Analyze what each screen is for and connect it to the correct existing screens.

For example:

Existing Warehouse List
→ New
→ New/Create Screen
→ Save/Create
→ Existing Warehouse List

If the second screen is part of the creation flow:

New
→ Screen 1
→ Continue / Next
→ Screen 2
→ Save / Create
→ Existing List

Use the actual content of the provided screens to determine the correct sequence.

Do NOT invent a different workflow.

========================================
3. CONNECT ALL BUTTONS
========================================

Review every interactive button/action on the 2 new screens.

Connect buttons such as:

- New
- Add
- Create
- Save
- Continue
- Next
- Submit
- Confirm
- Cancel
- Back
- Close
- Edit
- View

to the correct existing screen or state.

If a button should return to an existing screen, connect it to that SAME existing screen.

Do NOT create a duplicate destination.

========================================
4. SAME DESTINATION = SAME SCREEN
========================================

IMPORTANT:

If multiple buttons from different screens lead to the same destination, ALL of them must open the SAME existing screen.

Example:

Warehouse Screen A
→ New
→ New Part

Warehouse Screen B
→ Add Part
→ SAME New Part screen

Do NOT create two versions of the same screen.

Always reuse the existing destination.

========================================
5. SAVE / CREATE FLOW
========================================

After the user completes the new record and clicks Save/Create/Confirm:

Return to the appropriate existing Warehouse list/details screen.

The newly created item should logically appear in that existing list.

Do NOT create another list screen.

The flow should be:

Existing Warehouse Screen
↓
New
↓
Provided New Screen 1
↓
Provided New Screen 2 (if applicable)
↓
Save / Create
↓
Existing Warehouse List / Details Screen

========================================
6. CANCEL / BACK
========================================

If the user clicks:

Cancel
→ Return to the previous existing screen.

If the user clicks:

Back
→ Return to the previous logical screen.

If the user clicks:

X / Close
→ Close the modal if it is a modal, or return to the appropriate previous screen.

Do not break the existing navigation.

========================================
7. FIX SPACING AND ALIGNMENT
========================================

I manually worked on these screens, so some spacing may not be perfect.

Clean up the 2 provided screens to match the existing Warehouse design.

Fix:

- Margins
- Padding
- Gaps
- Card spacing
- Input spacing
- Button spacing
- Text alignment
- Field alignment
- Header alignment
- Table alignment
- Consistent widths
- Consistent heights
- Vertical spacing
- Horizontal spacing

IMPORTANT:

Keep my existing design.

Do NOT redesign the screens.

Only polish spacing, alignment, sizing, and consistency.

========================================
8. USE EXISTING COMPONENTS
========================================

Reuse the existing Warehouse components wherever possible:

- Header
- Sidebar
- Buttons
- Inputs
- Dropdowns
- Search
- Filters
- Sort
- Tables
- Cards
- Status badges
- Modals

Do not create visually different duplicate components if an existing component already exists.

========================================
9. SEARCH / FILTER / SORT
========================================

Analyze the 2 new screens.

If a screen contains a list/table where Search is useful:
→ Add Search.

If filtering makes sense:
→ Add the appropriate Filter.

If sorting makes sense:
→ Add the appropriate Sort.

Only add controls that are relevant to the actual data on the screen.

Do NOT add unnecessary Search / Filter / Sort controls.

========================================
10. REVIEW THE EXISTING WAREHOUSE FLOW
========================================

After connecting the 2 screens, review the surrounding Warehouse flow.

Make sure the navigation works correctly:

Role Selection
→ Warehouse
→ Existing Warehouse Screen
→ New
→ New Screen 1
→ New Screen 2
→ Save/Create
→ Existing Warehouse Screen

Also make sure existing actions such as:

View
Edit
Back
Cancel
Save
New
Close

still point to the correct existing screens.

========================================
11. DO NOT DUPLICATE ANYTHING
========================================

Before creating a connection, check whether the destination already exists.

If it exists:
→ Reuse it.

If multiple actions have the same destination:
→ Connect them all to the SAME screen.

Do NOT create:
- Duplicate New screens
- Duplicate List screens
- Duplicate Details screens
- Duplicate Warehouse flows

========================================
12. FINAL REQUIREMENT
========================================

The main goal is to FIX THE CURRENT "NEW" BUTTON and integrate the 2 provided screens into the existing Warehouse system.

The final flow should feel like ONE continuous system:

Warehouse
↓
Existing Screen
↓
New
↓
Provided Screen 1
↓
Provided Screen 2
↓
Save / Create
↓
Existing Warehouse Screen

Make all relevant buttons work.

Keep the existing design.

Fix spacing and alignment.

Reuse existing screens/components.

Do not create duplicates.

Do not create a new Warehouse flow.

The 2 screens I provide must become part of the EXISTING Warehouse flow.